// add shadow to an element
function addShadow(element) {
  element.classList.add("shadow");
}

// remove shadow from an element
function removeShadow(element) {
  element.classList.remove("shadow");
}
